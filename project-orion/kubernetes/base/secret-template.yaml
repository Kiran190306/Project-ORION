apiVersion: v1
kind: Secret
metadata:
  name: orion-secrets
  namespace: orion
  labels:
    app.kubernetes.io/name: orion
    app.kubernetes.io/component: secrets
    app.kubernetes.io/part-of: orion
type: Opaque
stringData:
  # ════════════════════════════════════════════════════════════
  # ORION Secret Template
  # ════════════════════════════════════════════════════════════
  # Replace values below with your actual secrets.
  # Do NOT commit plaintext secrets to version control.
  # Use external-secrets, SealedSecrets, or SOPS for GitOps.
  # ════════════════════════════════════════════════════════════
  ORION_DB_PASSWORD: "CHANGE_ME_DB_PASSWORD"
  ORION_REDIS_PASSWORD: "CHANGE_ME_REDIS_PASSWORD"
  ORION_API_KEY: "CHANGE_ME_API_KEY"
  ORION_JWT_SECRET: "CHANGE_ME_JWT_SECRET"
  ORION_ENCRYPTION_KEY: "CHANGE_ME_ENCRYPTION_KEY"
